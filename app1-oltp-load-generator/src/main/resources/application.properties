# Oracle Database Configuration for OLTP Application
# You can override these with environment variables

# Database Connection
db.url=jdbc:oracle:thin:@localhost:1521:ORCL
db.username=oltp_user
db.password=OltpPass123!

# Connection Pool Settings (optimized for high load)
db.pool.max=100
db.pool.min=20
db.timeout=30000
db.idle.timeout=600000
db.max.lifetime=1800000

# Application Settings
app.threads=50
app.batch.size=100
